apiVersion: v1
kind: Pod
metadata:
  name: userspace-vpp-pod
  annotations:
    k8s.v1.cni.cncf.io/networks: userspace-vpp-net-b, userspace-vpp-net-a
spec:
  containers:
  - name: vpp-vhost
    image: dpdk-app-centos:latest
    imagePullPolicy: Never
    securityContext:
      privileged: true
    volumeMounts:
    - mountPath: /etc/podnetinfo
      name: podinfo
      readOnly: false
    - mountPath: /run/
      name: shared-dir
    - mountPath: /dev/hugepages
      name: hugepage
    resources:
      requests:
        cpu: "2000m"
        memory: 16Gi
        hugepages-1Gi: 4Gi
      limits:
        cpu: "2000m"
        memory: 20Gi
        hugepages-1Gi: 4Gi
    command: ["sleep", "infinity"]
  nodeSelector:
    vswitch: ovs
  volumes:
  - name: podinfo
    downwardAPI:
      items:
        - path: "labels"
          fieldRef:
            fieldPath: metadata.labels
        - path: "annotations"
          fieldRef:
            fieldPath: metadata.annotations
  - name: shared-dir
    hostPath:
      path: /run
  - name: hugepage
    emptyDir:
      medium: HugePages
